#include "main.h"

/**
* swap int -> given two integers swap the values they are holding
*@a: parameter 1
*@a: parameter 2
*/
void swap_int(int *a, int *b);|
{
        int tmp;
        a = 7 b = 8
        tmp = *a;
        *a = *b;
        *b = tmp;
}
